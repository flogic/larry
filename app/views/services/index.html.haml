%h1 Services

- if @services.blank?
  %h3 No services.
- else  
  %ul
    - @services.each do |service|
      %li
        - if service.safe_to_delete?
          = link_to '[delete]', service_path(service), :method => 'delete', :confirm => "Are you sure you wish to delete the service '#{service.name}'?"
        = summarize(service)