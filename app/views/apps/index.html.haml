%h1 Apps

- if @apps.blank?
  %h3 No apps.
- else
  %ul
    - @apps.each do |app|
      %li
        - if app.safe_to_delete?
          = link_to '[delete]', app_path(app), :method => 'delete', :confirm => "Are you sure you wish to delete the app '#{app.name}'?"
        = summarize(app)