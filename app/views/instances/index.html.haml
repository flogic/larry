%h1 Instances

- if @instances.blank?
  %h3 No instances.
- else
  %ul
    - @instances.each do |instance|
      %li
        = link_to '[edit]', edit_instance_path(instance)
        - if instance.safe_to_delete?
          = link_to '[delete]', instance_path(instance), :method => 'delete', :confirm => "Are you sure you wish to delete the instance '#{instance.name}'?"      
        = summarize(instance)