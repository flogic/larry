.instance
  == #{brief(instance.customer)} -> #{brief(instance.app)} -> #{brief(instance)}
  - unless instance.description.blank?
    .description= instance.description
  - unless instance.hosts.blank?
    %ul
      %li== deployed to: #{list(instance.hosts)}
